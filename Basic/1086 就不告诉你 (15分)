#include<iostream>
#include<string.h>
#include<algorithm>
using namespace std;
int main()
{
	int num1,num2;
	char ans[8];
	cin>>num1>>num2;
	num1=num1*num2;
	sprintf(ans,"%d",num1);
	reverse(ans,ans+strlen(ans));
    sscanf(ans,"%d",&num1);
	printf("%d",num1);
    return 0;
}
